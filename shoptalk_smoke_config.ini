[llm]
model_name = gpt-4o
temperature = 0.1

[app]
personality = -1

[retrieval]
vector_db_output_dir = artifacts/smoke_test_vector_db
vector_backend = faiss
top_k = 10

[data]
product_blurbs = EDA/product_blurbs/smoke_blurb_dict.json
images_csv = smoke_images.csv

[server]
# auto binds to 0.0.0.0 in Docker and 127.0.0.1 outside Docker.
# Set an explicit address here to override auto-detection.
server_name = auto
server_port = 7860

[evals]
model_name = gpt-4o
temperature = 0.0
